# src_overrides

Files here mirror the Chromium source tree layout and are symlinked into `src/` by
`build/sync.py`. Use for NEW files (Bedrock components, branding resources). For edits to
existing Chromium files use `patches/`.
