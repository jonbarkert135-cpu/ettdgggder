# Bedrock — MemorySanitizer build (uninitialised reads). Requires an
# instrumented libc++; see docs/security/TESTING.md.
import("//build/args/bedrock-release.gn")

is_debug = false
is_official_build = false
symbol_level = 1
is_msan = true
msan_track_origins = 2
use_prebuilt_instrumented_libraries = true
